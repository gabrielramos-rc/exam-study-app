apiVersion: v1
kind: Service
metadata:
  name: exam-study-app-nodeport
  namespace: exam-study-dev
spec:
  selector:
    app: exam-study-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30001
  type: NodePort
